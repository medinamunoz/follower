<%= form_for(summary_phase) do |f| %>
  <% if summary_phase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(summary_phase.errors.count, "error") %> prohibited this summary_phase from being saved:</h2>

      <ul>
      <% summary_phase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
